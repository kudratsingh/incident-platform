# Session index

One line per session. **Read this before starting work**; it is cheaper than rediscovering.

`STATE.md` at the workspace root says where things *are*. This says how they got there, which is
the part that stops you from re-litigating a settled decision or re-investigating a closed
question.

Most of the campaign history is agent-side and lives in
`../incident-commander/context/INDEX.md`. This file carries what happened to **the platform**.
Read both — the interesting failures have been on the seam between them.

An archive listed as *transcript only* means the raw session data is on disk under
`~/.claude/projects/-Users-kudratsingh-Documents-audit-ws/` but was never packed. Pack it with
`./context/pack.sh <slug>` if you need it to survive.

| Date | Archive | What this session established |
|---|---|---|
| 2026-09-20 | — | **WO-R3-327 (ADR 0012's 2026-09-20 amendment): an append-only log has no way to say a take is over, so the reset had to say it.** The `/demo` page derives its phase strip from the newest `chaos.*` audit row. `audit_logs` is append-only by design, so after `make eval-reset` that row was still the *previous* take's kill: a freshly wiped world opened at **agent remediating** with `T+ 4m` counting from an incident that no longer existed, and the same leftover history reached the agent card, the metric latch and the DLQ "agent decided" badges — the last of those sharply, because the seeded dead-letter rows come back under **stable** ids, so last take's `replay_dlq_by_ids` names this take's row exactly. **The page cannot infer the boundary** — it is not in the audit log, the Redis keys are gone, and `agent_runs` says only that a run ended — so `reset_eval_state.py` now appends one `lab.world_reset` row, last, carrying that reset's whole counter summary as its payload (`world_reset_recorded` in the summary; the one count the row cannot make about itself). **The decision with a real alternative was the prefix.** `chaos.world_reset` would have been free — one prefix, one withholding rule, nothing to add — and was rejected because to the one consumer this row exists for, *the newest `chaos.*` row IS the fault*: a boundary filed there would be read as a fault, so the bug would have changed shape rather than gone. `chaos.` is the lab acting on the world; `lab.` is the lab acting on the timeline. `hidden_audit_action_prefixes` now excludes `lab.` wherever it excludes `chaos.`, under the identical `chaos:invoke` condition, and the payload is why the rule bites harder here than for a hook name: `chaos_keys_cleared` / `seeded_dlq_deleted` / `hot_set_reseeded` name every mechanism the lab sweeps, and the 2026-08-09 registry screen would not have caught it because this is a response body, not a description. **Principal = the evaluator's service account** (`SA_CHAOS_NAME`), resolved *within the seed tenant* so a second tenant's copy cannot raise from the tail of the reset (the WO-R2-18 shape), with a null `principal_id` on a stack where that account is unseeded — a boundary nobody signed beats no boundary. And unlike `record_tool_invocation` this writer is **not** savepoint-wrapped and does raise: there is no response to protect, and a reset whose boundary went unrecorded leaves the console reading the previous take as current. Console side, all pure functions in `demoPhase.ts`: the platform reading, the fault clock, the agent card's run, the breach latch and the DLQ badges read strictly newer than the newest boundary — a row sharing its timestamp belongs to the take being closed, and a run still **open** but older than it is deliberately kept, because that is a race with the reset's own sweep or a responder it could not reach and the disagreement is worth seeing on camera. The timeline draws the row as a **grey divider** rather than an event, split before grouping so a run of collapsible reads cannot hide the line inside an "N reads" group, and drawn from the always-fetched unfiltered stream so it survives the `lab` chip (whose server-side `action_prefix=chaos.` cannot carry two prefixes). With no boundary row — any stack older than this — the page reads the whole history exactly as before; an inferred boundary would be a guess about which rows to discard. 27 new tests (11 backend, 16 frontend), all red first. **NO TOOL SURFACE DELTA:** audit action names are not schema, no tool name, description, schema or scope moves, so there is nothing to rebless. |
| 2026-09-19 | — | **WO-R3-322 (owner decision O-31 D4, ADR 0031's 2026-09-19 amendment): a job whose every endpoint call failed now fails whatever made the endpoints fail.** WO-R3-220 shipped that rule gated on `chaos:downstream:bulk_api_sync` and filed the unconditional version as a follow-up, because it changes a production processor's outcome; the owner took the decision and this is it. One condition lost one clause (`if degraded is not None and ok == 0` → `if ok == 0`) and the flag now chooses only the *injection* — `fail` raises per call, `slow` answers late — never whether a sync that synced nothing was a success. The error message names the count: `bulk api sync failed: all N endpoint calls failed (0 of N endpoints returned a result)`, with the old clause kept inside it so existing readers still match, and nothing about the lab in it (ADR 0012 rule 1). **A partial failure is untouched** — still a completed job with `errors` counted in a result payload no operational tool reads, which is why the DLQ still under-counts a partly-broken dependency and the runbook now says so. **The consequence worth writing down is which objective moves.** `job_completion_rate` (99% over 24h): its denominator is jobs that settled `completed` or `dead_letter`, so a job that used to land in the first half can now land in the second, and on a quiet window one dead-letter is a 14.4× fast burn and therefore a `critical` alert. Retries spend none of it. `job_dispatch_latency` does **not** move, and the reason is the near-miss: `claim_for_running` stamps `started_at` on *every* claim, so a retried job's recorded latency is creation → its *last* attempt's start — with the default backoff (2s then 4s) that is inside the 30s threshold, but a deployment with a larger `job_retry_backoff_base` would eventually see retried jobs cross it. Organic exposure is 0.1^N per run (the default 5-endpoint job once in 100,000; a 1-endpoint job once in 10, and one in a thousand all the way to the DLQ). **No contract delta:** `degrade_downstream`'s description already said this happens under `fail` and is still true, so `tools/list` is byte-identical and nothing re-pins; the only value that moved is the text of `jobs.error_message`, which no tool schema, canned fixture or grader pins — it appears commander-side only inside an archived recorded world, which stays as recorded. Red-before was proved by running the four new tests against the unchanged processor (3 failed on the raise, the dispatcher one marked the job COMPLETED). The `degrade_downstream` integration test is unchanged by design: its flagged path is now a subset of the unconditional one. Also tightened: `test_processors.py` fixes the endpoint dice and resets the shared breaker per test, because with the raise unconditional a random all-three-failed roll (1 in 1000) or a breaker another test left open would have been a new flake. |
| 2026-09-19 | — | **WO-R3-289 (ADR 0033): two correct decisions left a fault that was real and unreadable, and the fix was a pattern already in the tree.** `saturate_db_pool` holds the **API/worker** process's connections on purpose (saturating the MCP pool would blind the reader — ADR 0031) and `get_postgres_health`'s `pool_*` fields describe **the process that answered** on purpose, saying so in every description (ADR 0030). Both right, and together the held pool was invisible: connections genuinely gone, acquisitions genuinely timing out, every tool the agent has reading healthy. ADR 0030 named it ("what this does not close"), ADR 0031 filed it as D1, and WP-8.5's `db_pool` scenario has been grading on downstream effects since. **The fix is ADR 0030's own mechanism turned on pools:** each process publishes its pool to `pool:state:<process>` (a closed pair, `api_worker` and `mcp`), the read fails *known*, and `get_postgres_health` gains a `pools` group beside the five untouched `pool_*` fields with an empty group always carrying `pool_gauges_unknown_reason` — because an empty list that reads as "no process has a pool problem" is the confident wrong answer the whole order exists to remove. **What took the thinking is where the two patterns must differ: a pool reading is a *sample* where a breaker's state is *latched*.** So the TTL is 60 s, not 24 h — a process that stops publishing has to drop out of the listing rather than freeze at its last healthy number, which makes absence itself a reading — and a publisher task per process writes it rather than a hook on a state change, because a pool moves with no event to hang off. **Both obvious cadences were wrong and that is the finding worth keeping:** `metrics.start_metrics_emitter` is a no-op outside production, so riding it means the gauge never runs in the only environment it exists for; and the worker's `_metrics_loop` is a member of ADR 0027's closed enum, so `pause_control_loop('metrics')` would let one lab hook blind the reading another lab hook exists to produce. Publishing at boot rather than after the first interval for the same class of reason. Rejected: reading the worker's pool from `pg_stat_activity` (it counts connections to the database, not a pool — no `size`, no unopened overflow, and nothing at all about callers that waited and gave up, which is the counter that separates saturated from busy), and a key per process *instance* (recorded not solved: two replicas of one role would overwrite each other's record, which compose and the eval world never run and a grader would rather not see as a variable-length list). Also caught in the build: `process` is a reserved `LogRecord` attribute, so `extra={"process": …}` raises `KeyError` — the fail-open write was not failing open until a test proved it. 23 new unit tests, 10 on the pin, 5 API, 4 integration on a real Postgres + real Redis (`test_pool_gauge_cross_process`, in the CI census). **TOOL SURFACE DELTA (field-level, no count change):** `get_postgres_health` gains `pools` + `pool_gauges_unknown_reason` + the `PoolGaugeReading` `$defs`, and `saturate_db_pool`'s description changes one clause that this order makes false — a release and a commander re-pin, which is not this packet's. |
| 2026-09-19 | — | **WO-R3-310/311/315 (ADR 0036): three gaps in `make eval-reset`, and they are the same gap.** Each is state outside the namespaces the reset sweeps, with nothing in the reset's own output to say so — so all three were found by CI's fresh stack disagreeing with a warm volume rather than by anyone reading the code. The lesson worth keeping is the counter, not the three steps: the reset's JSON is what `make eval-reset` prints and what a readiness note quotes, so a step with no count is a step nobody can tell ran, and a static tripwire now fails if one is added without one (`hot_set_reseeded`, `breakers_reset`, `agent_runs_closed`). **The breaker half is the one with a design in it.** `breaker:state:*` is a platform key (ADR 0030) so the `chaos:*` sweep never carried it, and one `degrade_downstream` left `bulk-api-sync` reading open for the record's whole 24 h TTL — in every world recorded after it. Deleting the key is wrong twice: the registry is a module-level dict in a process the reset cannot restart (ADR 0006), so the breaker writes the same failure straight back, and an *absent* record reads as unknown rather than closed, so a world audit has nothing to assert. So the reset rewrites each record closed with the failure fields null and raises `breaker:reset:at`, which every breaker honours before it publishes again and before it refuses another call. **The signal carries a time, and that is the whole correctness argument:** a counter cannot answer "is what I remember older than the reset", so a counter-shaped signal wipes a legitimate post-reset failure on the very publish that reports it — the test that caught this in design is `test_a_failure_after_the_signal_stands`. Ordering is the other half: raised after the rewrite, a breaker publishing in between makes the world worse than leaving the key alone. Cost is one Redis GET beside an existing SET (the check rides the publish that was already happening, so ADR 0030's hot-path rule holds) plus one per refused call while a breaker is open; a closed breaker asks nothing. `agent_runs` rows are closed `failed` with a `closed_by: reset` entry appended to their own append-only `phase_history` — no column added — and never deleted, because the console replays them and the briefing is the responder's own words. **Divergence from the order:** WO-R3-310's premise is false on master — the seeder has re-seeded the hot_set unconditionally since 4ff14bb (2026-08-01); what was missing was any statement of it, and the durability half (it is the one fixture written with a TTL, which is what `volatile-*` eviction takes first) is left alone deliberately because the TTL is a value the commander's canned fixtures and drift ledger read back. No tool surface change, so no contract delta. 28 new unit tests, 4 new integration tests on a real Redis (`test_eval_reset_breakers`, in the CI census). |
| 2026-09-19 | — | **WO-R3-313: the `/demo` console, and the two things a live screen must not do.** The page (`/demo`, support+, `docs/DEMO.md`) puts the world, the agent and the audit log on one screen at a 2s cadence via a new `usePolling` hook — the third live-data pattern beside `useJobStream` and `useAsyncData`, and the one that needed a rule written down: `loading` goes true on *every* tick, so a polled panel keys its skeleton off `loading && data === null` or it flashes a skeleton twelve times a minute (the Audit tab, now on 5s, was exactly that). **The finding worth keeping is the recovery rule.** The phase strip's platform-side witness reads the audit log plus the mode's metric, and the metric is inside its threshold *both* before the fault lands and after it is fixed — lag is 0 in a healthy world and 0 again after a successful restart. A rule asking only "inside the bar?" flashes *recovered* during the seconds between injecting the fault and it becoming visible, i.e. it lies about the one moment the recording exists for. So recovery requires a **breach to have been observed first**, latched per fault row and reset when a new one appears; the honest cost is that a page opened after the fix says `fault injected`, which is why `make demo-live` prints the URL at baseline. Second rule, from decision C: the agent's word (`agent_runs.state` — the only source that can tell planning from remediating, because a plan leaves no mark on the platform) and the platform's reading are **never merged**; when they name different stations the strip shows both, because a merged reading asserts a state neither witness gave and the disagreement is the shape of INC-001…003. Derivations are pure functions in `utils/demoPhase.ts`, driven through every transition from fixtures. Also: `GET /api/v1/audit/logs` gains `action_prefix` (the repository already supported it — `action=` is an exact match and cannot isolate a stream), the DLQ "agent decided" badge is read off `agent.tool_invoked` only so the lab's own seeding is not attributed to the agent, and the console now ships as `ghcr.io/<owner>/incident-platform-console` beside the backend on the same version, with `nginx.conf` turned into a template whose `${API_UPSTREAM}` lets one image serve this repo's compose (service `app`) and the commander's demo compose (service `api`). 127 frontend tests (was 58), 11 audit API tests. Built against WO-R3-312's shapes while that order was unmerged and rebased onto it the same day; the guesses that were wrong are worth naming because they are the shape of every parallel build — the nine reportable states are the commander's own `IncidentState` values (`triage`, and an `awaiting_approval` that needed a station of its own even though nothing reaches it yet, because a state with nowhere to land reads as something else), the lag reading's reason field is `lag_unknown_reason` and its `recent_samples` arrive **newest first** (fed to a chart as given, the series' span goes negative), and `/admin/circuit-breakers` has to be carried whole because an empty list with `unknown_reason` set is a different finding from an empty one without it. The tolerant envelope normaliser written for the unmerged period was deleted at the rebase rather than kept: every endpoint's envelope is now known (`items` for the two paginated ones, the whole response for the other two), and a normaliser that outlives its uncertainty is a place for a real mismatch to hide. |
| 2026-09-19 | — | **WO-R3-312 (the live demo, platform half, ADR 0035): the audit log was a read surface for the new table under another name, and that was the whole finding.** The console needed two facts that existed only inside the responder's process — what it is doing now, what it concluded — and inferring them from the audit log is wrong in exactly the moments a demo is about (`get_consumer_lag` is read three times while investigating and three times while verifying, and no row tells those apart). So the responder **reports**: `agent_runs` plus two `[commander: telemetry]` MCP tools under a new sixth scope `agent_runs:write`, and human operators read it over REST. **What took the thinking:** "no read tool for `agent_runs`" is easy and is not the rule. Every MCP call writes an audit row carrying its arguments, and `list_audit_events` is on the responder's own read surface — so without a second decision it could have read back every state, hypothesis and confidence it had reported. Two changes close it: the calls audit as their own `agent.run_reported` stream (not `agent.tool_invoked`, because a status report is not something the responder did *to* the platform, and an operator timeline would have coloured it as one), and `hidden_audit_action_prefixes` gains a rule pointing the **opposite way** from the chaos one — that stream is hidden *from* the principal holding the write scope. Same function, inverse condition. Routing the row in the `tools/call` envelope rather than writing it from the tool is also what gives a report the R2-51 guarantee (an unauditable report rolls back) — a tool-level row would have been savepoint-swallowed. **The second decision worth keeping:** the input field is `run_label` and the column is `scenario`, because ADR 0012's registry screen bans that word from any non-chaos tool's `tools/list` surface and a new exemption was declined — the responder's principal holds the scope, so it can read these descriptions, and whether its model does depends on a filter in the other repo. The screen stays green with no exemption. **Coordinator correction mid-build:** the state enum is the commander's own nine `IncidentState` strings verbatim (`triage` … `awaiting_approval` …), not the eight the order drafted, so the reporter maps nothing. Four write rules, each because a console reads live: upsert by run id, phase history that appends on a state *change*, a terminal state that closes the run and refuses a later report (409), a briefing written once (409). `app/core/consumer_lag.py` extracted so the REST twin computes the same lag from the same function — `app.api` may not import `app.mcp`, and two copies of "is this lag known?" is a console and an agent that can disagree. 88 new unit/API tests, 5 new integration tests in the CI census (`test_agent_runs_postgres`). Recorded, not solved: `agent_runs` is not swept by the eval reset, and nothing here can tell a responder that stopped reporting from one whose reporter failed — no heartbeat, by design. **TOOL SURFACE DELTA:** `+report_agent_run` / `+report_agent_briefing` (38 → **40** tools, chaos unchanged at 15, read tier unchanged at 16), one new scope, one new audit action — a release and a commander re-pin, which is not this packet's. |
| 2026-09-19 | — | **WO-R3-218 (WP-8.2, ADR 0034): the cheaper mechanism was the unobservable one, and that is the whole finding.** A1 is "queries slow, pool fine", and the plan's evidence for it (`p95_query_ms_1m`) is permanently null — ADR 0030 decided that and said why. So the evidence is restated as `longest_active_query_ms` rising and `active_queries_over_slow_threshold` above 0 while `pool_wait_timeouts_1m` stays 0 and `pool_checked_out` is normal, and that restatement **chooses the mechanism**: those two readings come from `pg_stat_activity`, and an application-level delay (the order's cheaper option, mirroring `inject_latency`'s sleep) is not a running query, so it moves neither. It would have shipped a fault with no signature. `slow_db_queries` therefore writes `chaos:db_query:slow` and a chaos-only worker task keeps **real** reads of one declared relation running, each held open by a server-side `pg_sleep`. **What took the thinking:** one sleeper makes the reading a sawtooth — `pg_stat_activity` reports the age of the statement running *now*, so for the first 500 ms of every chunk the over-threshold count reads 0, and a fixture that is wrong a quarter of the time is worse than none. Two sleepers offset by half a chunk fix it by construction (the oldest in flight is never younger than `query_ms·(n−1)/n`), which is where `MIN_QUERY_MS = 1100` comes from, and the inequality is pinned by a test against both constants. **Why the pool reading stays clean:** the sleepers run in the worker's pool and the agent's reading is the MCP process's own, so the "pool fine" half falls out of where the fault lives rather than being arranged. **Residue is bounded by the chunk, not the TTL:** a single `pg_sleep(ttl_seconds)` would have held a connection for an hour past the reset; chunks with the flag re-read between them leave at most `query_ms` (2 s default, 10 s ceiling) after the sweep — the one chaos key whose effect outlives the reset at all. `target` is a closed three-member enum mapped to relations by a dict, so an undeclared target is refused and a hand-written flag cannot reach the statement text. Stated, not solved: the platform's own jobs still run at normal speed (job durations, lag, outbox age, SLOs untouched) — a slow database, not a slow platform — and armed together with `saturate_db_pool` the two hooks leave three free connections rather than four, a combination no Family A world declares. 50 new unit tests, 9 new integration tests in the CI census (`test_db_query_latency`, which reads on a second engine to prove the cross-process property and samples across 2.5 chunks to prove the reading never dips). **TOOL SURFACE DELTA:** `+slow_db_queries` (37 → **38** tools, 14 → **15** chaos; read tier unchanged at 16) — a release and a commander re-pin, which is not this packet's. |
| 2026-09-18 | — | **WO-R3-225 (WP-10.0, ADR 0032): a fault that survives its own fix.** Every hook before this one was ended by the agent's first correct action, so `remediate_verify_fails` and every retry scenario were canned-only by design. `kill_consumer` gains `sticky` (default false — the plain kill is byte for byte what it was), which writes `chaos:kill_sticky:{group}` holding the absolute deadline `ttl_seconds` gives. The kill-state read fetches flag and marker in one MGET and, when the flag is gone and the window is open, puts it back with `PXAT` at that same instant. **The design decision that took the thinking:** whether `restart_consumer_group` should fail to find the key, or find it and be overruled. It finds it. The action is **unmodified** — it deletes the flag, answers `kill_key_cleared: true`, and the supervisor finds the flag re-armed before it restarts anything — because the alternative (a separate key the action never sees) makes it answer `kill_key_cleared: false`, which that tool's own description reads as a reused `idempotency_key` replaying an old success. The agent would be told it had made a mistake it had not made, in the one scenario that grades how it reacts to a failed action. Teaching the action about the sticky key was rejected outright: that is ADR 0012 rule 1's leak at a different angle. **Absolute, not rolling**, in two independent ways — the deadline in the marker's value and the marker's own Redis TTL — so a world's recovery time is not a function of how many times the agent retried. Recorded rather than solved: the hook computes the deadline in the MCP process and the worker compares it, so clock skew shifts it (immaterial on one host, and Redis's TTL bounds it anyway). **No tool-count change** — the whole rebless delta is field-level, and the behaviour change is in a tool this PR does not touch. Integration test runs the real supervisor, the real action and the real reset sweep against a real Redis, because `PXAT` and expiry are the mechanism and a fake would be testing the fake. |
| 2026-09-18 | — | **WO-R3-219 + WO-R3-220 (WP-8.3/8.4, ADR 0031):** two Family A hooks, both flag-and-reader like every hook before them. `saturate_db_pool` holds pooled connections in the **worker** process, not the MCP one — saturating the pool the tool surface itself needs would blind the reader rather than produce a fault; the clamp always leaves four connections acquirable, so the eleven loops slow rather than stop. `degrade_downstream` makes the simulated `bulk-api-sync` endpoints fail or run late, and the shipped breaker (threshold 3, recovery 30 s) does the tripping. **What cost time to find:** the fault had no observable at all. A job's per-endpoint errors live in a result payload no operational tool reads — `get_trace` returns status/retries/error text and `search_traces` filters type and status — so the breaker opened and every tool still read healthy. Hence the one behaviour change: while the flag is set, a job whose every endpoint call failed fails. Both hooks are mechanism-only until WO-R3-217 publishes pool and breaker state across processes (divergence H7); `test_downstream_latency.py::test_the_same_breaker_reads_closed_from_another_process` is that gap written as a test. |
| 2026-09-17 | — | **WO-R3-272:** ADR 0027 records owner decision O-23: retain the verified eleven-member pause enum while adding scenarios only for contrast families; consumer-group families use `kill_consumer` with a loop pause where needed. |
| 2026-08-08 → 08-10 | *transcript only* | **The audit.** Platform's half of 129 defects across both repos. `AUDIT_REPORT.md` at the workspace root. |
| 2026-08-10 → 08-12 | *transcript only* | **The fix campaign and v0.5.0.** Platform work orders merged, tag cut, image published. Tool surface went 26 → 27 tools; the rebless diff was verified to the leaf as exactly 10 scalar deltas and zero structural changes. |
| 2026-08-12 | *transcript only* | **ECS deploy gated** (PR #96). The deploy job now requires `master` + `push` + `vars.ENABLE_ECS_DEPLOY == 'true'`, so a fork or a stray branch cannot reach the cluster. |
| 2026-08-13 | *transcript only* | **Seeding bug found from the agent side.** `SEED_EVAL_FIXTURES` was set on the `platform` service, which overrides `command:` to run the standalone MCP process and so never executes the REST app's startup hook that reads the flag. Nothing had ever seeded the demo stack. |
| 2026-08-16 | `2026-08-16-campaign-backfill.zip` | **This convention, plus a backfill.** `context/` added to both repos; the whole campaign's 175 transcripts packed. The archive is identical in both repos — transcripts are workspace-level, not repo-level, and it predates the convention. Future archives go in whichever repo the session worked in. No product code changed. |
| 2026-08-21 | *transcript only* | **`get_cache_key_info` read tool.** Closes the readiness-sweep finding that `create_stale_cache` writes a Redis key no read tool can see. Exact-key existence / TTL / type / size (never the value), `telemetry:read`, allowlisted to the same four prefixes `invalidate_cache_key` may delete; subset relations tripwired in `test_cache_key_allowlist.py`. Chaos-enabled MCP surface 27 → 28 — the next re-pin's rebless diff gains `+get_cache_key_info`. |
| 2026-08-21 | *transcript only* | **`create_stuck_dag` chaos hook.** `remediate_runaway_saga_success` could not run honestly live: the boot-seeded DAG auto-completes ~10s after boot, so a live run graded alert-credulity, not remediation. The hook manufactures `upstream (completed) → root (dead_letter) → N (waiting)` — stuck by the platform's own rules, since the resolver promotes only when every parent is `completed` and `dead_letter` is terminal. Observable via `get_dag_state`; compensators `replay_dlq_by_ids` (unsticks) and `pause_dag` (stabilizes) per the ADR 0008 pairing rule. Chaos-enabled MCP surface 28 → 29 — the rebless diff gains `+create_stuck_dag`. |
| 2026-08-30 | *transcript only* | **The integration tier was never running.** `testpaths` scoped bare `pytest` to unit+api and the three `RUN_*` gates were exported nowhere, so all 25 tests in `backend/tests/integration/` — the only proofs of RLS tenant isolation, audit-log immutability and outbox single-writer exclusivity — had never executed in CI. Added the `integration` job (Testcontainers, all gates set, census step that fails on any skip) + `make test-integration`, and popped `ALEMBIC_DATABASE_URL` in both `_alembic()` helpers so an inherited value can't redirect the destructive migration cycle. Also deleted the `test` job's Postgres/Redis service containers, which nothing collected had ever read. |
| 2026-08-30 → 09-07 | *transcript only* | **What the first paid live run taught the platform.** Three things, all found by running the commander against a real stack rather than by reading code. (1) *RLS fail-closed works, and the proof is a refusal.* The hand cleanup of stray alerts was rejected when issued as an unscoped `UPDATE alerts …` over the `incident_app` role — exactly the wave-11 R2-129 behaviour, arriving unprompted on a statement nobody had written a test for. It is the only positive evidence we have that the policy binds writes the campaign never anticipated; the cleanup went through as superuser instead. (2) *The SLO evaluator counts the eval fixtures, so a freshly booted eval world alerts on itself.* 4 failed of 7 seeded jobs is 42.9% against a 99% objective — a 14.4× fast burn by construction, raised within one `SLO_EVALUATION_INTERVAL_SECONDS` (300s default) of boot, and re-raised every hour because `_fast_burn_dedup_key` buckets by hour. Mitigated commander-side for the run by setting `SLO_EVALUATION_INTERVAL_SECONDS=0` in the eval compose; the platform fix — exclude fixture-seeded rows from the SLI denominators — is filed as **WO-R2-132**. (3) *`reset_eval_state.py` does not return the alert surface to baseline.* Its sweep predicate is `source LIKE 'chaos:%'` and the SLO loop writes `source = 'slo:<id>'`, so organic alerts survive every reset and accumulate as agent-visible distractors — filed as **WO-R2-131**. |

| 2026-09-07 | *transcript only* | **The lab contradicted itself, and an honest agent lost.** Live run `efdc3b2a9864` seeded a stuck chain `remediation_hint=replay_safe` whose root's `error_message` said `SchemaValidationError: payload missing required field 'user_id'`. The agent read both, judged a missing required field permanent, and escalated instead of replaying — sound, and graded a failure. In the lab nothing validates payloads, so the hint was the truth and the text was decoration; the wire said nothing about which to believe. Every lab writer (`seed_dlq_messages`, `create_stuck_dag`, `poison_message`, `create_bad_data_job`, the four seeded rows and their `job_triages` rows) now draws from one table, `app/lab/dlq_failure_stories.py`, with `coherence_violations()` as the rule and a table test walking each writer's pairs through it (**WO-R2-146**). Same PR applies commander #191's four DAG description deltas — `get_dag_state` is no longer "the verification surface for pause_dag", `replay_dlq_by_ids` names the DAG-root un-stick path, `pause_dag` says it is a stabilizer that expires and blocks the fix, `list_dlq_messages` says it is the only read carrying a hint (**WO-R2-141**). Rebless deltas: four description strings plus the error/triage texts on the seeded pack. |
| 2026-09-08 | *transcript only* | **A poisoned message is not safe to replay.** Reviewing the 2026-08-31 trajectory of `remediate_dlq_backlog_success`, the user found the agent replaying a row whose error read `SchemaValidationError: payload missing required field` — because `poison_message` labelled its own dead-letter row `replay_safe`. It always had. WO-R2-146 had already touched this pair and picked the wrong half: it moved the *text* to `UpstreamTimeout …` so the row would agree with its hint, which made the row self-consistent and left it describing a transient fault the hook never injects. **WO-R2-166** moves the hint instead. `poison_message` now writes a schema-violation text under a `NULL` hint by default (nothing has classified a freshly poisoned message — triage is off by default) or `human_required` on request; `replay_safe` is not in its input vocabulary at all. It also gains the `create_bad_data_job` shape — declared `fixture_name`, deterministic id in its own `eeeeeeee-dead-…` namespace, idempotent-until-drifted with the refusal raised *before* the Kafka send, and `payload.seeded_fixture` so the reset DELETEs the row instead of cancelling it. Second half: a new chaos hook `create_mislabeled_dlq_job` writes the lab's one sanctioned incoherent row (hint `replay_safe`, permanent bad-data text) behind a required `mislabel: true`, for a commander scenario whose premise is that the classifier lied — declared outside `DLQ_FAILURE_STORIES` and reachable only through `sanctioned_incoherent_story()`, with a test asserting the coherence screen still flags it. Chaos-enabled MCP surface 29 → 30; rebless gains `+create_mislabeled_dlq_job`, `poison_message`'s two input / three output fields, two 409 refusal codes, and the not-replay-safe behaviour change. |
| 2026-09-08 | *transcript only* | **`max_retries` was a cap on runs, and 3 bought two retries.** The user read `backend/app/workers/dispatcher.py` and found the ladder retrying while `new_retry_count < max_retries`: with the default 3 a job runs three times — the original plus two retries — and dead-letters on the third failure. The code already knew; the retry message on that same path printed `attempt {n}/{max_retries}`, and the dead-letter message said "exhausted after 3 attempts". Only the name said retries, and a reader who believed it budgeted one run more than the platform has ever given. **WO-R2-172** fixes the name and keeps the arithmetic — the comparison, the default, the backoff and every runtime decision are byte-for-byte what they were. `jobs.max_retries` → `jobs.max_attempts` via Alembic `f1c7b93a4d26` (a rename, data preserved, reversible; verified both directions against a real Postgres); `MAX_JOB_RETRIES` → `MAX_JOB_ATTEMPTS` with the old env name accepted for one release (used with one deprecation warning when it is alone, refused at boot when both are set and disagree). The two public shapes go additive rather than breaking, per CLAUDE.md's own Kafka-rename rule: the `job.failed`/`job.dlq` event and the REST `JobResponse` carry `max_attempts` **and** `max_retries` with the same value, the old one marked `deprecated` in the JSON Schema and in OpenAPI, and every reader prefers the new name and falls back. The word for the count is now **attempt** everywhere — `attempt {n} of {max}` on the retry path, "exhausted after N attempts" unchanged, and the lab's seeded `attempt 3/3` texts left exactly as they were and still true. **The MCP tool surface is byte-identical** (`tools/list` dumped from the registry on both sides, same sha256, 30 tools) — no re-pin, the eval stays on v0.6.3. |
| 2026-09-15 | *transcript only* | **The eval world can run with the SLO evaluator on.** Two coupled fixes, both filed off the paid sequence. **WO-R2-132:** the seeded fixtures *are* an SLO breach — 4 of the 5 terminal jobs the seed writes are dead-lettered, which is 80x the `job_completion_rate` budget, so a fresh boot raised a critical fast-burn alert within one `SLO_EVALUATION_INTERVAL_SECONDS` and re-raised it hourly on the next dedup bucket. The eval world's answer was to switch the evaluator off (cmd #180), which also switched off the only non-chaos producer of the alert the agent under test is woken by. `app/services/slo.py` now excludes rows carrying a top-level `eval_fixture` or `seeded_fixture` payload marker from both halves of every objective: a row a lab script INSERTed in a terminal state was never dispatched, ran, failed or completed, so counting it measures the seeder. Same judgement `_DISPATCHED_STATUSES` already makes about cancellations. Dialect-branched exactly as `_delete_seeded_dlq_fixtures` is — JSONB containment on Postgres (not a `::boolean` cast, which raises on `{"eval_fixture": "banana"}` and would abort the evaluation pass), `json_extract` on the SQLite harness, `COALESCE` on both arms so a NULL payload does not empty the denominator. **WO-R2-131:** `_resolve_chaos_alerts`' predicate was `source LIKE 'chaos:%'` and the evaluator writes `slo:<objective-id>`, so organic alerts survived every reset — three stray ones aborted run C pre-spend on 2026-08-31. The new `_resolve_organic_alerts` enumerates the **baseline** instead of the producers: it resolves every active alert whose id is not one of the five `stable()` fixture alerts, read from `seed_eval_fixtures._alert_rows` so the list cannot drift, which covers the next producer on the day it ships. Resolved, never deleted. New summary key `organic_alerts_resolved`; `chaos_alerts_resolved` and its test are untouched. 12 new tests (6 unit SLO, 3 evaluation end-to-end built from the seeder's own specs, 3 reset) plus 3 Postgres integration tests for the two dialect-branched statements. **The MCP tool surface is byte-identical** (`tools/list` dumped from the registry on both sides, same md5, 30 tools) — no re-pin; this rides the uncut v0.6.4. `SLO_EVALUATION_INTERVAL_SECONDS` is still `0` in the commander's `demo/compose.yml`: raising it is a commander-side change with a live world audit after it, deliberately left out of this PR. |
| 2026-09-15 | *transcript only* | **The audit log was telling the agent who broke the platform.** `list_audit_events` returned the `chaos.tool_invoked` / `chaos.tool_denied` stream to any `incidents:read` principal, with `extra_data` naming the hook and the arguments it was fired with, and `get_trace` returned the same rows for the trace a chaos invocation ran under. ADR 0012 rule 1 had screened descriptions and schemas since v0.4.9 — nothing had ever screened a response body, which is why a leak this loud survived six releases. **WO-R3-187 (owner decision O-4).** The read tools now pass `app/services/operator_audit.py::hidden_audit_action_prefixes` into `AuditRepository.list_logs(exclude_action_prefixes=...)`: the `chaos.` stream is visible only to a principal holding `chaos:invoke`, the exclusion runs in SQL so `total` counts only readable rows, and asking for the withheld stream — by prefix or by exact action — is an empty page, never an error (a refusal would confirm what is being withheld). **The predicate was inert until the tokens split**, which is the other half: the live `incident-commander` account held `chaos:invoke` (divergence G3), so `scripts/seed_incident_commander.py` now mints two — the agent (`PLATFORM_TOKEN`, reads + `actions:execute`, and the seeder *removes* `chaos:invoke` if it finds it, announced on stderr) and the evaluator (`PLATFORM_CHAOS_TOKEN`, reads + `chaos:invoke`) — both labelled in one banner, in one transaction. 34 new tests: the visibility rule on both tools for both principals, a registry-parameterised sweep calling every read tool on a chaos-seeded world and screening the serialized response for the substring `chaos` (it fails by name on a read tool it has no call for, so a future tool cannot skip it), and every chaos hook refusing the agent's scope set, walked from the chaos package rather than listed. **The MCP tool surface is byte-identical** (`tools/list` dumped from the registry on both sides, same sha256, 30 tools) — the chaos wording had already left the agent-visible description in plat #195, so the rebless delta is behavioural, not textual. Two response-side leaks stay open by decision and are inventoried with tripwires in the sweep: `bad_deploy`'s alert `source` / `title` (owner decision O-8 — renaming it without moving `_resolve_chaos_alerts`' `source LIKE 'chaos:%'` predicate re-creates WO-R2-131) and the `(chaos poison_message on topic …)` suffix on `poison_message`'s DLQ error text (a coupled commander fixture rebless). Descriptions remain out of scope: ADR 0016 defers principal-scoped `tools/list`, and masking the `[chaos:` prefix would empty every commander scenario's chaos validation (divergence G4). |

| 2026-09-16 | WO-R3-251 | Poison-message errors retain the schema-failure story and topic but describe producer correction without naming the hook. Response sweep covers the poisoned row including its internal payload markers; poison exception retired. No read tool emits the payload marker; bad-deploy O-8 unchanged. |
| 2026-09-17 | WO-R3-254 (platform half) | **The lag reading now carries time.** The alert says lag is CLIMBING and the only sensor returned one undated number, so the claim was unverifiable by a caller that cannot let time pass: live run `42000dfda188` re-read `get_consumer_lag` three times in 25s, got 29/29/29, concluded "not moving" and escalated. The metrics loop now writes `kafka:consumer_lag:worker-dispatcher:samples` beside the value — a JSON list, newest first, of the last 5 `{lag, measured_at}` measurements, same 90s TTL refreshed on each write, so the pair is fresh-or-absent together. The value key keeps its exact shape and meaning; `check_backpressure` is untouched, and the sample write is best-effort so a Redis refusal on the window cannot cost the value write. `get_consumer_lag` gains `measured_at`, `age_seconds` and `recent_samples`, and its description says the trend comes from ONE call: samples arrive ~every 60s, so a re-read inside that window returns the same number and that repetition means "not re-measured yet", never "not moving". Nothing is invented — a group reporting a recorded constant has no measurement time (nulls + empty window, with the reason on the wire), an absent window returns the current value with a null `measured_at`, and a window whose newest sample is not the current value dates nothing. `reset_eval_state._clear_lag_samples` deletes the window between runs (new summary key `lag_samples_cleared`) while leaving the loop-owned value key alone, so a trend measured in one run is never the first thing the next run reads. 30 new tests; ADR 0012 needs no exemption — the lab-vocabulary screen and the response sweep pass unchanged. **TOOL SURFACE DELTA:** three new output fields + one `LagSample` `$defs` entry + a rewritten description, so this release forces a commander re-pin and rebless (that half is WO-R3-254's commander leg). |
| 2026-09-17 | comment pass C | **Short docstrings across the main flow — comments only, no code.** 35 files under `backend/app/` plus `scripts/seed_incident_commander.py` gained one- or two-line module, class and method docstrings saying what each piece is *for*: the eight Kafka consumer groups and their `handle_message`, the dispatcher's hand-off pair, the job / auth / saga / alert / SLO / idempotency / service-account services, the `jobs`, `users`, `audit_logs` and enum modules, the job, audit, outbox and idempotency repositories, the request dependencies, the admin router and `tools/list`. Nothing existing was rewritten and files that already explain themselves were skipped — most of `app/mcp/` is in that group, since a tool's module docstring plus its `description=` already carry the story. **No Pydantic class gained a docstring**, because a model's docstring becomes the `description` of its JSON Schema and would move the pinned tool contract; the proof is that the 30-tool `tools/list` dump and the full OpenAPI document are byte-identical to `origin/master` (same sha256 on both). Verified comment-only by AST comparison with docstrings stripped, plus ruff, `mypy -p app` (157 files) and 1622 unit + API tests. |
| 2026-09-17 | *transcript only* | **Public-docs refresh.** Docs-only pass over everything a stranger reads. Counts that had drifted and were corrected against the code: nine background loops → **eleven** (README, CLAUDE.md, ARCHITECTURE.md all disagreed with `worker_loop`, which starts 8 consumers + 11 loops); 7 CloudWatch alarms → **10**, 7 runbooks → **8**; migrations "11, head e1d24a8b50c2" → **30, head f1c7b93a4d26**; "twelve of thirteen phases shipped" → **nine of fourteen**; Phase 12 was marked 🟡 in the milestone plan while the status table said Complete. Hard test counts (726 = 447+254+25) were **removed** rather than re-pinned, following README's own stated policy — they were stale by ~2.3× and had been refreshed three times before. Substantive additions: `docs/ADR/README.md` (all 26 with statuses and what amended what), `docs/README.md` (one line per doc), a README section on the agent-facing MCP surface (it was invisible in the README), and `DATA_MODEL.md` sections for the four tables it never documented despite promising every table — `alerts`, `deploy_markers`, `idempotency_records`, `service_accounts`/`service_account_tokens`. Two accuracy fixes worth knowing: the seeder's **default** agent scopes are `telemetry:read` + `incidents:read` with **no `actions:execute`** (docs claimed otherwise; a pre-split account keeps it through the union-on-rerun, a fresh one does not, and every Tier-1 tool requires it), and the admin console has **eight** tabs, not six. Known-stale and deliberately left for a follow-up: `.env.example` is missing ten live settings including `KAFKA_BOOTSTRAP_SERVERS`, `ANTHROPIC_API_KEY`, every `LLM_*` flag and `CHAOS_ENABLED`; and `ci.yml`'s integration census still names only five of the eight files. |
| 2026-09-17 | *transcript only* | **No license is granted yet.** The owner decided (2026-09-17) that the code is not licensed for anyone's use for now, so the README stopped saying otherwise. The `## License` section body was `MIT`; it now states that no license is granted at this time, that the code is published for reading and review only, that all rights are reserved, and that using, copying, modifying or redistributing it needs written permission from the author. Docs only — no LICENSE file added, none wanted. A repo sweep found no second MIT or open-source claim to fix: `pyproject.toml` and `frontend/package.json` declare no `license` field at all (`frontend/package.json` is `"private": true`), and the only other "licence" hit in the tree is the ordinary English word in ADR 0001. |
| 2026-09-17 | WO-R3-259 | Complete the environment template from every Settings field with safe defaults and commented optional secrets; add a coverage test. CI census now names all eight integration modules and is checked against the test directory. |
| 2026-09-17 | WO-R3-267 (platform half) | **The cache-key sensor can now show that a key is stale.** An alert named a key as stale and the only sensor for it returned `exists / type / ttl_seconds / size` — identical readings for a current hot-set entry and one that no longer matches the database, a few bytes apart, with `get_redis_health` adding only a Redis-wide miss ratio. The platform was asserting what it could not show, and two paid live runs of `remediate_stale_cache_success` split on byte-identical readings (green at 0.75 confidence one day, drift to 0.55 and an escalation the next). `get_cache_key_info` gains `records_referenced` / `records_found`: entries in these namespaces are copies of job records Postgres holds, so the tool resolves the records the entry names — the items in its stored list, or the single job its key names, read from the key via the new `_cache_scope.job_segment` so a `cache:job:` payload is never opened — and counts how many the database still holds for the caller's own tenant, through the new tenant-scoped `JobRepository.existing_ids_for_tenant` (ids only, no row loaded). A current hot-set entry reads 3 referenced / 3 found; the one `create_stale_cache` writes reads 3 / 0. **The hook is unchanged** and nothing marks the entry as stale: the hook already puts the world in that state by writing names no record answers to, and the tool measures it at call time — which also means no side-car to keep consistent, so the reset's existing re-seed from `_dlq_specs()` is the whole consistency story (pinned by a new test that asks the tool's own resolver, plus a negative control). Both fields are null, never 0, when the platform cannot work out what an entry refers to (a counter, a collection type, a JSON object, an unparseable value, a list too long to resolve), and the DB half degrades through `degrade_on_db_error` so a failed lookup nulls both rather than reporting references with no counterpart. Rejected, with reasons in the module docstring and `docs/REDIS.md`: `written_at`/`age_seconds` (no writer records one, and age reads backwards here — the healthy entry is the old one), `source_newer` from the cached row's `updated_at` (`JobResponse` does not carry it), and `OBJECT IDLETIME` (this tool's own `STRLEN` is an access, so the first call destroys the signal). 21 new tests; ADR 0012 needs no exemption — the lab-vocabulary screen and the response sweep pass unchanged. **TOOL SURFACE DELTA:** two new scalar output fields and a description change on one tool, no input change and no `$defs` entry (pinned by `tests/unit/test_cache_key_info_shape_delta.py`), so this release forces a commander re-pin and rebless — that half is WO-R3-267's commander leg. |

| 2026-09-17 | WO-R3-200 | **One hook pauses one background loop, and the enum of loops had to be corrected first.** The eval needs the contrast to `kill_consumer`: same symptom (jobs accepted, nothing executing), opposite evidence — a stalled outbox relay leaves the backlog in Postgres with `worker-dispatcher` lag flat, where a killed dispatcher leaves it in Kafka with lag climbing. The relay is not a consumer group, so no hook could reach it. `pause_control_loop(loop_name, ttl_seconds)` writes `chaos:pause:<loop>` and each loop reads its own key once per iteration. **The plan's eight-member enum was wrong in two ways, both verified against the code before building** (divergence report H2/H3): three members — `dependency_resolver`, `saga_coordinator`, `read_model` — are Kafka **consumer groups**, not loops, and `kill_consumer` has stopped any consumer group by its group id since Wave 1, so they are dropped and a caller using those names now gets an invalid-params refusal; and `_resume_unblocked_waiting_loop` was missing, which Family C cannot work without — it promotes a stranded child within ~10s, so stalling the resolver alone does not strand anything. Shipped enum = the **eleven** loops `worker_loop` actually starts, no consumer groups. The enum is a safety boundary so it is asserted, not documented: `test_pause_control_loop.py` parses `dispatcher.py`, collects the loops `worker_loop` starts, and asserts a bijection with the enum plus, per member, that the matching loop calls `loop_is_paused` with that member and no other — a twelfth loop cannot ship unpausable. Three insertion points are decisions, each with a test: **after** `worker_tick()` in the delayed-retry loop (that call is the whole worker's heartbeat, so a single-loop pause must not report the process wedged), **inside** the outbox relay's leader gate (ADR 0020 — checking first would move leadership for a reason unrelated to leadership), and fail-open on a Redis error (a blip must not stall the relay in production; the strict fail-closed variant exists only to decide a *restart*, and the TTL ends this pause). `CHAOS_ENABLED` short-circuits before any Redis call, so production pays one boolean per loop per tick. The result returns `tick_interval_seconds` for the loop asked about, because the intervals span 0.5s to an hour and the TTL caps at 3600 — a pause on `digest` or `idempotency_reaper` can expire before a tick reads it, and the platform's fourth description rule applies to a hook too. Recorded consequence of including `lease_renewal`: a pause outliving `STALE_RUNNING_THRESHOLD_SECONDS` lets the stale-RUNNING sweep dead-letter live jobs, which the TTL does not undo. Teardown needs nothing new — `reset_eval_state.py` sweeps `chaos:*` and the namespace property is now asserted for the new helper. 51 new tests (40 unit + 11 API) + 3 integration; the response-side leak sweep now screens every read tool against a world with `chaos:pause:outbox_relay` set. **TOOL SURFACE DELTA:** `+pause_control_loop` (30 → **31** tools, 11 chaos) and a fifth `BlastRadius` member `single_loop` — the first change to that closed enum since Step 0 — so this release forces a commander re-pin and rebless. ADR 0027. The read tool the contrast needs (`get_outbox_status`) is WP-4.2, not this packet. |
| 2026-09-17 | WO-R3-201 (platform half) | **The outbox stall is now readable, and the relay says when it last ran.** ADR 0027 made the relay stoppable; nothing let the agent SEE the resulting world, so both halves of the Family B contrast looked identical from every tool it had. `get_outbox_status` (no arguments, `telemetry:read` — the same scope as `get_consumer_lag`, so one token sees both halves or neither) returns one live reading: `unpublished_count`, `oldest_/newest_unpublished_at` + their ages, `unpublished_past_attempt_limit`, `last_publish_at` + `seconds_since_last_publish`, and `relay_last_tick_at` / `relay_heartbeat_age_s` / `relay_heartbeat_known` / `relay_heartbeat_unknown_reason` / `relay_tick_interval_s`. **Four decisions that are not obvious and each of which the easy option gets wrong.** (1) *The heartbeat had to be built, not derived.* The plan said "relay heartbeat age" without a source; the only existing candidate is the CloudWatch gauge pair, emitted at most once every 60s (`_OUTBOX_GAUGE_INTERVAL`) on a loop that polls every second — it could not tell a relay that stopped five seconds ago from a healthy one. So `_outbox_relay_tick` now stamps `outbox:relay:last_tick` (Redis, 24h TTL) on every pass it completes, **inside** the tick and after the fetch: written in the loop body it would mean "the coroutine is alive", and a loop that keeps turning while skipping its work is exactly the state the signal exists to expose. Outside `chaos:*` on purpose — platform signal, platform writer, and the reset sweep must not clear it. (2) *`last_publish_at` excludes abandoned rows.* `mark_failed` stamps `published_at` as well as `failed_at` (ADR 0001 item 3), so a bare `max(published_at)` reports a fully stalled relay as having just delivered — silently, and it is the worst answer the tool could give. (3) *Every age is the database's clock.* Counts, timestamps and `measured_at` come from one statement including `now()`, so an age and its timestamp cannot disagree; the relay's own pass time is the one value from another host's clock and the description says so, with a negative age clamped to 0 rather than printed. (4) *A healthy outbox reads healthy, and its one alarming-looking reading is named:* on a quiet platform `last_publish_at` can be hours old with `unpublished_count: 0`, which is silence, not a stall — the description tells the caller to read the pair. Unknown is null with a reason, never a fabricated 0 (a 0 heartbeat age reads as "the relay ticked as you asked"). Counts are tenant-scoped; the relay pass time is platform-wide, and the description separates them. 37 new tests (31 unit + 5 API + 1 more sweep parameterisation); the response-side leak sweep now calls the new tool against a world with `chaos:pause:outbox_relay` set AND a stale `outbox:relay:last_tick`, so it is screened on the very world it describes rather than on the unknown branch; `test_outbox_stall.py` now runs the tool's own repository query instead of re-implementing it. **TOOL SURFACE DELTA:** `+get_outbox_status` (31 → **32** tools, still 11 chaos) — a contract delta, so this release forces a commander re-pin and rebless. ADR 0028. The commander half (ReadToolName literal, registry entry, output model, `RESOURCE_ARG_FIELDS`, snapshot) follows the release and is not this packet. |
| 2026-09-17 | WO-R3-213 | **Stranding a `WAITING` child takes two stalls, and the plan named one.** Family C's discriminator is an absence — a child stuck `WAITING` with nothing dead-lettered and nothing paused, so the correct answer is to escalate rather than to replay (`create_stuck_dag`) or to un-pause (`pause_dag`). Divergence H3 is that the world is not reachable by stalling the `dependency-resolver` consumer group alone: `_resume_unblocked_waiting_loop` exists to backstop exactly that and promotes the child within ~10s, by design. **Both stall mechanisms were already shipped** — `kill_consumer('dependency-resolver')` since Wave 1 (divergence H2: it is a consumer group, not a tick loop, which is why it is not a `ControlLoopName`) and `pause_control_loop('resume_unblocked_waiting')` in WO-R3-200 — so this packet is the **proof, the teardown guarantee and the decision record**, not new mechanism: zero lines changed under `backend/app/`. Two new test files. `tests/integration/test_resolver_stall.py` (3 tests, real Postgres) builds the composed world and reads it back through the agent's own tools: `get_dag_state` says parent `completed`, child `waiting`, `paused` false with `paused_by` null and `created_at` 47 minutes old; `list_dlq_messages` is empty. `tests/unit/test_resolver_stall_world.py` (8 tests, real rows on SQLite) proves the gate and both directions of the necessity claim — a `job.completed` redelivery to a *live* resolver promotes the child even with the sweep paused, and `_check_chaos_kill` answers true for the group id the resolver is really constructed with. **The window is counted in sweep iterations, not wall seconds, and every stall assertion ships with its own control.** The test requirement asks for a window longer than `_RESUME_SWEEP_INTERVAL` because a shorter one would pass with the pause removed; a single 10-second wall wait is one tick, so the interval constant is patched down and the loop is run for twelve iterations (two minutes of sweep time) while the *same* window, unpaused, promotes the child. That control is the red half, and removing the pause check from the sweep reproduces exactly three failures. One harness finding worth keeping: cancelling the loop task from outside can land inside the sweep's transaction, and an invalidated aiosqlite connection is replaced by a reconnect — which for an in-memory database is a fresh, empty one, surfacing as `no such table: jobs` from the assertion afterwards. The loop is stopped by raising `CancelledError` out of the pause check instead, at the top of an iteration. **The recovery is asymmetric and only one TTL matters:** the resolver returning heals nothing, because the parent's `job.completed` is already consumed, so the *pause's* TTL is the only thing that un-stalls the world and a teardown clearing only the kill key would leave a permanently stranded child. `make eval-reset`'s one `chaos:*` scan reaches both keys, now asserted of that exact pair. **ADR 0027 self-amended:** pausing this one member suspends the correctness backstop ADR 0011 and ADR 0022 rely on — bounded by a TTL, swept between scenarios, fully undone by expiry (unlike `lease_renewal`), and unsafe in production for a reason worth naming, since a stranded `WAITING` child raises no alert, dead-letters nothing and shows in no queue depth. Also recorded: `single_loop` is honest about the process but the sweep is cross-tenant, so the pause holds promotions for every DAG in the environment while it lasts. `ci.yml`'s integration census gains `test_resolver_stall` (the unit-tier guard `test_ci_census_covers_every_integration_module` caught the omission first, which is the census working). **NO TOOL SURFACE DELTA:** `tools/list` dumped from the registry on both sides is byte-identical — 32 tools, sha256 `0fa5c4c1175c…`. Follow-up not built because the order does not ask for it: generalising WP-4.2's per-pass heartbeat to "last tick age per loop" would let the agent tell a child nothing has reached yet from one nothing is coming for, instead of inferring it from `created_at` against its own clock. |
| 2026-09-18 | WO-R3-274 + WO-R3-275 | **Three of Family C's five worlds were unreachable, and the boot-seeded DAG was never a fixture.** Plan 01 §7.2 needs `resolver_stall` (root `completed`, child `waiting`, no DLQ row), `downstream_child_failed` (root `completed`, a descendant dead-lettered) and `paused_dag` (`get_dag_state` reads `paused: true`). None could be built: `create_stuck_dag` always dead-lettered the root, and `pause_dag` declares `actions:execute`, which the evaluator principal deliberately lacks (ADR 0012) while `ChaosHook` refuses a non-chaos tool name — unreachable by construction, not merely hard. **The tempting fixture is a drained DAG.** `dag-parent-job` → `dag-seed-job` → `dag-child-job` is seeded with a `completed` parent, so the resolver (or the resume sweep, within ~10s) promotes both children on first boot; `make eval-reset` re-anchors their timestamps from `_dag_specs()` and never restores their statuses, and because those specs carry `run_seconds: None` the re-anchor writes NULL `started_at`/`completed_at` onto rows that are by then `completed`. **Restoring the statuses is the wrong fix** — the reset clears `chaos:*` first and the sweep ticks every 10s, so a restored `WAITING` row races the promotion that is about to happen again. The spec was made honest instead: one paragraph in the reset's docstring plus a unit test pinning both halves (the paragraph, and the seed still declaring those two rows `waiting` with no dispatch times). **`create_stuck_dag` gains three inputs**: `root_status` (`dead_letter` default = the old chain byte for byte, `completed` = stranded), `child_age_seconds` (0..86400, backdates **every** row — moving only the descendants would make a child older than its own parent), and `failed_step` (only with a completed root). **The honesty finding worth keeping:** the completed-root chain does NOT hold by itself. A `completed` root leaves step-1 with no unmet parent, so both promoters drain it — the composed stall from WO-R3-213 is still required, and the description says so rather than advertising a fault that evaporates before the agent probes it (the fourth description rule, the `poison_message` `replay_safe` lesson). The integration tier asserts it in the direction that hurts: the same twelve-iteration window drains the chain unpaused and holds it paused. `failed_step` is the exception that holds alone, because no `waiting` row in it has a `completed` parent — which is also why its descendants **before** the failed one are `completed`: the order text said "the rest wait", which is right for N=1 and incoherent for N≥2 (a job cannot have exhausted its retries while its own parent is still `waiting`). **`pause_dag_chaos` writes the operator's own flag.** `app/utils/dag_pause.pause_key_for` is imported, not copied; the value stays `"paused"`; the TTL default and bounds are `pause_dag`'s (600s, 1..3600), so a lab pause taken with default arguments IS an operator pause. It is therefore **the one chaos hook whose keys sit outside `chaos:*` on purpose** — a `chaos:` key would not be the key the resolver and `get_dag_state` read — so its teardown is `_clear_dag_pauses` (`dag_pauses_cleared`), which already existed for agent residue, plus the TTL. Asserted as an *equality*: both pauses taken on two identical chains in one test, the whole `get_dag_state` output compared with ids normalised away, so a field added to that output later is covered the day it ships. The integration tier proves the pause is **enforced**, not merely reported: an unpaused sweep window declines to promote a chain the lab paused, which takes the real ancestor walk and keyset query. **One asymmetry recorded rather than hidden:** an operator pause writes `agent.tool_invoked`, a lab pause writes `chaos.tool_invoked`, which is withheld — so the agent sees no audit row where it would have seen one. An absence, not lab vocabulary; forging the operator row would be a lie in the ground-truth stream. 44 new tests (26 unit + 12 API + 6 integration); `ci.yml`'s census gains `test_stranded_chain`; `docker-compose.yml`'s pinned chaos enumeration goes 11 → 12. **TOOL SURFACE DELTA:** `+pause_dag_chaos` (32 → **33** tools, 12 chaos), `create_stuck_dag` input `+root_status +child_age_seconds +failed_step` and output `+step_job_ids +dead_letter_job_id` (`waiting_job_ids` narrowed to mean what it says), description rewritten — so this forces a release and a commander re-pin. No new refusal code. Behaviour change with no field to point at: a chain this hook manufactured can now exist with **no dead-letter row at all**. ADR 0029. |
| 2026-09-18 | WO-R3-217 (platform half) | **Two readings were promised that this platform cannot take, and one that it could not see across a process boundary.** Family A needs the agent to tell slow queries from a saturated pool from a failing downstream dependency; `get_postgres_health` returned a ping, a connection count and nothing else. Three tools now answer it, and the interesting part is what is *not* built. **Divergence H7, closed:** the breaker registry is a module-level dict and the only breaker (`bulk-api-sync`) is created in the process that runs the worker loops, while the MCP reader is a separate boot from the same image (ADR 0006) — a tool walking that dict would have read its own empty one and reported every breaker closed, which is the confident-wrong answer the description rules exist to stop. Each breaker now records its state under `breaker:state:<name>` (Redis, platform namespace so the reset leaves it alone, 24h TTL, written on every state change and at most once a minute while calls flow) and gained a wall-clock `last_state_change_at` beside the monotonic `_opened_at` plus a failure **class** — `timeout` / `connection` / `other`, never the message, because a message can carry a job id, a URL or the name of whatever injected the fault. `get_circuit_breakers` reads the records: an absent breaker is *absent from the listing*, never reported closed, and an unreachable store returns an empty listing with `unknown_reason` set. **Divergence H6, decided and reported rather than worked around:** `pg_stat_statements` is not installed anywhere in either repo, and enabling it is not this packet's to do — without `shared_preload_libraries` a `CREATE EXTENSION` migration *fails*, and the eval world's Postgres command line lives in the commander's compose file. The deeper finding, which the plan does not state: **the extension could not produce `p95_query_ms_1m` even if it were enabled.** The view is cumulative per normalised statement since the last reset, with no percentiles and no way to narrow to a minute, so a "p95 over one minute" derived from it would be a percentile across per-statement means over an unstated window. So `p95_query_ms_1m` and `slow_query_count_1m` ship as the plan specifies them and are null in every response, with one of three fixed reasons saying which case it is — and two readings that *can* be taken are added beside them: `longest_active_query_ms` and `active_queries_over_slow_threshold` (500 ms, reported), both from `pg_stat_activity`, therefore server-side and the same answer whichever process asks, and both proved movable by a real `pg_sleep` in the integration tier. That is the lesson from 2026-09-07 applied before the spend rather than after it: a discriminating signal the lab cannot move is a fixture defect. **The pool numbers describe the pool of the process that answered the call, and every field description says so** — the API and worker processes size their own pools and are invisible from the read surface, which is a live constraint on WP-8.3's hook and is recorded in ADR 0030 rather than solved here. `pool_wait_timeouts_1m` needed building too: SQLAlchemy keeps no such counter and has no pool event for a checkout timeout, so `CountingQueuePool` overrides the one method that raises it and keeps a rolling 60-second window per process (a test fails loudly if a SQLAlchemy upgrade moves `_do_get`). `get_slo_status` surfaces `compute_all` unchanged — same objectives, same windows, same arithmetic — with an unbounded burn rate rendered null rather than an infinity JSON cannot carry, the `14.4x` paging threshold beside every rate so the number can be read, and the trap named in the description: `total: 0` means nothing settled in the window, so full budget is an absence of evidence, not a healthy platform. 68 new unit tests in four files plus two API sweep parameterisations (1866 unit + API green), two new integration files in the CI census (`test_breaker_visibility` proves the cross-process read on a real Redis with two independent clients; `test_postgres_health_pg` proves the Postgres branch and pins the extension-absent reason). ADR 0030. **TOOL SURFACE DELTA:** `+get_slo_status` `+get_circuit_breakers` (35 → **37** tools — WO-R3-219/220's two chaos hooks landed first — still 14 chaos; read tier 14 → **16**) and twelve new `get_postgres_health` output fields — a release and a commander re-pin, which is WO-R3-217's other half. |

## Things a future session should not have to rediscover

- **When a fixture's hint and its text disagree, check which half describes what the hook actually
  did before deciding which half to move.** (2026-09-08, WO-R2-166.) WO-R2-146 found
  `poison_message` pairing `replay_safe` with a `SchemaValidationError` and repaired it by moving
  the *text* to a transient one. The pair became coherent and the row became a different lie: the
  hook injects a schema violation and nothing else, so a row claiming an upstream timeout described
  a fault that never happened, and still invited a replay of a payload no replay can fix. The
  coherence screen cannot catch this — it compares two fields to each other, not either field to
  the code. Only a reader asking "what does this hook do?" catches it, which is why the question is
  written down here.
- **A claim about what a caller may safely *do* is held to a higher bar than a claim about what a
  field contains.** `replay_safe` is the strongest routing claim this platform's vocabulary has, and
  it sat on a chaos hook's row for four releases. Same rule, stated for tool descriptions, is now
  the fourth bullet in CLAUDE.md's "Tool descriptions — normative" section.
- **The lab may lie exactly once, by name.** `create_mislabeled_dlq_job` is the only writer allowed
  to produce an incoherent row, because "the classifier was wrong" is a real failure an agent has to
  be measured against. Three guardrails keep the exception from becoming a loophole, and all three
  matter: the pair is declared outside `DLQ_FAILURE_STORIES` so nothing can resolve it from a hint;
  its text is the CSV bad-row one, never the `SchemaValidationError` string from run
  `efdc3b2a9864`, so the promise that *that* pair is never written again stays absolute; and a test
  asserts `coherence_violations` still flags it. Adding a second sanctioned exception should feel
  hard — if it does not, one of the three has been dropped.
- **A lab fixture whose fields disagree grades correct reasoning as wrong.** `remediation_hint` and
  `error_message` arrive together on one `list_dlq_messages` entry, and so does the `job_triages`
  block. Any new lab writer takes its text from `app/lab/dlq_failure_stories.py`; adding a text
  beside a hint by hand is how WO-R2-146 happened. If a wording trips
  `coherence_violations()`, reword it — do not loosen the screen. ADR 0012 says the lab must not
  *name* itself on the agent's surface; this is the same rule one layer in, where it must not
  *contradict* itself either.

- **A screen on descriptions is not a screen on responses, and the data is the louder half.**
  ADR 0012 rule 1 was enforced from v0.4.9 by a registry test over every tool's description and
  schemas, and it was doing its job — plat #195 had already scrubbed the word `chaos` out of
  `list_audit_events`' description. The tool went on returning `chaos.tool_invoked` rows naming the
  hook and its arguments for six more releases (WO-R3-187). When a rule says "do not reveal X",
  check every surface the caller can read, and prefer a screen that calls the thing and inspects
  what came back: `backend/tests/api/test_read_tools_never_name_the_lab.py` is that shape, and it
  fails by name on a read tool nobody gave it a call for.
- **A count is a disclosure.** The same fix had to move `total` as well as the rows: a `total` of 12
  over a 4-row page tells the reader that eight rows exist which it may not read, and `truncated`
  built on it says the page was capped when it was actually filtered. Withholding therefore belongs
  in the `WHERE` clause, never in a list comprehension after the query.
- **A permission filter keyed on a scope is inert while one token holds every scope.** The chaos
  audit filter keys on `chaos:invoke` and the live agent account held it, so the filter would have
  shipped as a no-op for exactly the runs it was written for (divergence G3). The platform half and
  the token split are one change; check who actually holds the scope before believing a predicate
  keyed on it does anything.
- **The digest that matters is the index digest, not the child.** `docker manifest inspect -v`
  returns a list whose `[0]` is the linux/amd64 *child* manifest. Pinning that is wrong and the
  mistake was made once — the commander must pin `sha256:8b57d0c9…`, the index. Verify with
  `docker buildx imagetools inspect`, which shows both and labels them.
- **`tools/list` needs no rows.** The only CI job that boots the stack diffs the tool schemas, so
  an empty database is indistinguishable from a fully seeded one. This is why the seeding bug
  survived the entire project. Any check meant to catch missing *data* has to actually read data.
- **Severity is a closed enum and 32 of the commander's 38 scenario alerts violate it** — they
  send `high` / `medium` / `low`, which the platform rejects. Not a platform bug, but it is the
  platform's enum that decides it, so a change here re-calibrates every scenario over there.
- **`AlertPayload` declares two fields the webhook never sends.** This is the mechanism behind
  issue #141 (alert dedupe inert in production).
- **Nothing produces a `fingerprint`.** All 38 commander scenarios pin one and the agent derives
  incident identity from it, but the `alerts` table has no such column and the webhook payload has
  no such key. The ingress contract the eval exercises has no producer behind it.

## Open, not blocking

- [#141](https://github.com/kudratsingh/incident-platform/issues/141) — alert dedupe inert in production.
- [#142](https://github.com/kudratsingh/incident-platform/issues/142) — webhook v2 signing.

## Standing rules that outlive any session

- **The commander is an external client.** No shared code imports, no direct connections to this
  platform's Postgres, Redis, or Kafka. A capability the agent needs is a platform PR that adds a
  tool, never a bypass.
- **Chaos hooks are env-gated** and must stay that way.
- **In a git worktree, run pytest with `PYTHONPATH=<worktree>/backend`.** The venv installs the
  backend editable via a `.pth` that hard-points at the *main* checkout, so a full-suite run from a
  worktree root imports `app` from `master` instead of your branch. It looks like a test-ordering
  bug — new tests pass when run from `backend/`, then fail or vanish on the full run — and it cost
  one session its whole budget. CI is unaffected: it checks out a single tree.
- **A green pytest run is not evidence the tier ran.** pytest exits 0 on a fully-skipped module,
  which is how `backend/tests/integration/` stayed invisible for the whole campaign. Four of the
  eight files skip unless `RUN_RLS_TEST` / `RUN_EVAL_RESET_TEST` / `RUN_MIGRATION_LOCK_TEST` are
  set; the other four skip without a Docker daemon. Run them with `make test-integration`, never
  bare `pytest backend/tests/integration/`. The `integration` CI job parses its own JUnit report
  and fails if any test skipped — keep that step, it is the only thing standing between this tier
  and a second silent decade.
- **The demo stack is shared state.** `docker compose -f demo/compose.yml down` keeps volumes;
  only `make demo-destroy CONFIRM=1` deletes them. An agent trimming a Kafka topic here once
  crash-looped three consumer groups.
